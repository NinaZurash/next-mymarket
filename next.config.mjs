/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "fakestoreapi.com",
      "img.freepik.com",
      "auth.tnet.ge",
      "static.my.ge",
    ],
  },
};

export default nextConfig;
